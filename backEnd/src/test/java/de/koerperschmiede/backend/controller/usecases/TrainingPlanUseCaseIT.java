package de.koerperschmiede.backend.controller.usecases;

import de.koerperschmiede.backend.controller.BaseIT;

public class TrainingPlanUseCaseIT extends BaseIT {

    // TODO: add use case tests

    // given user with training plan, admin deletes training plan, check that every custom exercise also got deleted

    // check specific if logic is correct to update exercise list and remove not used custom exercises
}
